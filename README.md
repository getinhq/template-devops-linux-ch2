# Server setup automation

## Task
Write `server_setup.sh` to install common tools, create a sudo user, configure SSH keys, and optionally harden SSH/ufw.

## Starter (not finished)
Script exits until implemented. Test on a VM locally.

## Your work
- Document variables at top of script (username, packages).
- Idempotent where possible.
- Bonus: disable root SSH login, enable ufw rules (document in README).
